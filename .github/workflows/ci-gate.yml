name: CI Gate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  gate:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
 fix/pr1-system-adjust
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}

 main
      
      - name: Run CI checks
        run: |
          chmod +x scripts/ci/*.sh
          bash scripts/ci/run_all.sh

