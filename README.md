# Aether3D

Aether3D 是一个基于 **Gaussian Splatting** 的本地 3D 重建与渲染系统，目标是在 **最少照片、最短时间** 内生成 **高质量、可交互的 3D 空间结果**。

当前仓库处于 **Phase 0.5（Guardrails）** 阶段，重点不是功能，而是**架构稳定性、版本可回滚性和多人协作安全性**。

---

## 当前阶段

- ✅ **Phase 0**：稳定基线（Frozen）
- 🔒 **Phase 0.5**：流程与回滚护栏（Guardrails）
- ⏳ Phase 1：核心功能开发（尚未开始）

> Phase 0 对应 Git Tag：`phase0`  
> 该 Tag **永不移动，仅用于对照和回滚参考**。

---

## 核心开发约束（非常重要）

### 分支与提交规则

- ❌ **禁止直接向 `main` 分支 push**
- ❌ **禁止对 `main` 分支进行 force push**
- ✅ `main` **只接收 Pull Request**
- ✅ 所有破坏性修改 **必须使用 `git revert`**
- ❌ 禁止使用 `git reset` / `git rebase` 改写历史

> main 分支的历史 **只允许向前追加**

---

### Phase 0 保护规则

- ❌ 禁止修改 Phase 0 基线代码
- ❌ 禁止 reset / rebase 到 Phase 0
- ✅ Phase 0 仅通过 `phase0` tag 访问（只读）

---

## 标准开发流程（强制）

所有开发必须遵循以下流程：

1. **Preflight 检查**（提交前自检）
2. 从 `phase0.5` 创建新分支
3. 开发（单一职责）
4. 更新相关文档（如有）
5. 提交（一个 commit 只做一件事）
6. 提交 Pull Request
7. Review + CI
8. 合并到 main
9. 打 Tag（如进入新阶段）

---

## 分支命名规范

- `phase1/*`：Phase 1 功能开发
- `feat/*`：通用功能
- `hotfix/*`：紧急修复
- `release/*`：发布准备

---

## 回滚哲学（必须遵守）

- ✅ **任何错误都可以修**
- ❌ **历史不能被改**
- ✅ 回滚 = 新的 commit（`git revert`）
- ❌ 不允许“假装没发生过”

错误示例（禁止）：
```bash
git reset --hard
git rebase
git push --force

正确示例（允许）：

git revert <commit>
git push


⸻

快速开始（开发者）

# 从 Phase 0.5 创建功能分支
git checkout phase0.5
git checkout -b phase1/camera-capture

# 创建阶段性 Tag（只在 main）
git tag -a phase1-alpha -m "Phase 1 alpha checkpoint"
git push origin phase1-alpha


⸻

说明
	•	当前仓库 不追求功能完整
	•	当前目标是：每一步都可回滚、可验证、可继续
	•	所有 Phase 的边界定义请参考：docs/PHASES.md
	•	所有工作流规则请参考：docs/WORKFLOW.md



